# Project Registry

This folder stores robot project registries.

Current registry:

```text
project/hexapods.json
```

The Hexapod Projects page updates this file automatically when opened through the local server:

```bash
node server.js
```

Then open:

```text
http://localhost:8000/hexapod-projects.html
```
